# SPDX-FileCopyrightText: 2026 Toon Verstraelen <Toon.Verstraelen@UGent.be>
# SPDX-License-Identifier: Apache-2.0

# The Universe package checker, over the package as it would be submitted.
name: lint

on:
  workflow_dispatch:
  push:
    branches:
      # Check the package for a change on the main branch ...
      - main
    paths:
      # ... and only if relevant files have changed.
      # The checker reads the subtree that is submitted to `typst/packages`,
      # so this list must stay consistent with the `exclude` field of `typst.toml`.
      - src/**
      - tools/build_package.py
      - typst.toml
      - README.md
      - CHANGELOG.md
      - LICENSES/**
      - .github/workflows/lint.yml
  pull_request:
    # Check the package on pull requests ...
    paths:
      # ... only if relevant files have changed.
      - src/**
      - tools/build_package.py
      - typst.toml
      - README.md
      - CHANGELOG.md
      - LICENSES/**
      - .github/workflows/lint.yml

concurrency:
  group: lint-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  package-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # The checker reads the subtree that is submitted to `typst/packages`, which is the
      # tracked files minus the `exclude` list of the manifest, and that is not the working
      # tree. So it is pointed at a copy of that subtree rather than at the checkout.
      - run: ./tools/build_package.py --destination build/package
      # The checker runs as a container rather than through its own GitHub Action,
      # because that action expects credentials for a GitHub App
      # that a personal repository does not have.
      - run: >-
          docker run -v ${{ github.workspace }}/build/package:/data
          ghcr.io/typst/package-check check
